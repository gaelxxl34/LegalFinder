import 'package:flutter/material.dart';

class LegalAdvices extends StatelessWidget {
  const LegalAdvices({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Legal Advices"),
        centerTitle: true,
      ),
    );
  }
}
