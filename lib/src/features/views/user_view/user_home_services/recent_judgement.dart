import 'package:flutter/material.dart';

class RecentJudgement extends StatelessWidget {
  const RecentJudgement({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Recent Judgement"),
        centerTitle: true,
      ),
    );
  }
}
